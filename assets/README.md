# Reference Asset Inventory

The original ChatGPT research attachment contained:

- `good/`: 8 user-selected Good Case images
- `bad/`: 3 user-selected Bad Case images

The attachment binaries and exact filenames were not available in this packaging workspace. Their observed content and aesthetic judgments are preserved in `../references/examples.md`.

When the originals are available, place them in these directories without changing the Skill instructions:

```text
assets/good/good-01.<ext> ... good-08.<ext>
assets/bad/bad-01.<ext> ... bad-03.<ext>
```

Do not substitute generated look-alikes for the user's references. Keep any descriptive original filenames if they are useful; the numbered names are only a fallback convention.

